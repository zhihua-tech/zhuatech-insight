# Insight 架构

版权所有 © 2026 上海如静知华信息科技有限公司。

Vue 管理端和分析师工作台访问 Spring Boot API。业务层管理分析任务、语义指标、数据连接、结论审核和使用反馈，JWT 与角色策略控制数据域权限。

模型通过 `AiProvider` 接入。生产部署应让查询服务只访问受控语义层，执行行列级权限、敏感字段脱敏、查询限额、SQL 审计和结果证据留存。
